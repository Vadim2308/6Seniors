/**
 * Cинтаксис
 */
{
    let func = (arg1, arg2, ...argN) => arg1;
    let double = n => n * 2;
    let sayHi = () => alert("Hello!");
}
{
    //Является сокращенной версией от FE
    let func = function(arg1, arg2, ...argN) {
        return arg1;
    }
}